# Application bindings
$terminal = uwsm app -- $TERMINAL
$browser = omarchy-launch-browser

# Core apps
bindd = SUPER, return, Terminal, exec, $terminal --working-directory="$(omarchy-cmd-terminal-cwd)"
bindd = SUPER, F, File manager, exec, uwsm app -- nautilus --new-window
bindd = SUPER, B, Browser, exec, $browser
bindd = SUPER SHIFT, B, Browser (private), exec, $browser --private
bindd = SUPER, M, Music, exec, omarchy-launch-or-focus spotify
bindd = SUPER, N, Editor, exec, omarchy-launch-editor
bindd = SUPER, D, Docker, exec, $terminal -e lazydocker
bindd = SUPER, O, Obsidian, exec, omarchy-launch-or-focus obsidian "uwsm app -- obsidian -disable-gpu --enable-wayland-ime"

# Remove bindings for apps not installed in this fork
unbind = SUPER, G              # Signal
unbind = SUPER, slash          # 1Password
unbind = SUPER, C              # HEY Calendar
unbind = SUPER, E              # HEY Email
unbind = SUPER, Y              # YouTube
unbind = SUPER ALT, G          # Google Messages
unbind = SUPER, X              # X
unbind = SUPER SHIFT, X        # X Post

# Keep WhatsApp
bindd = SUPER SHIFT, G, WhatsApp, exec, omarchy-launch-or-focus-webapp WhatsApp "https://web.whatsapp.com/"

# ChatGPT (kept)
bindd = SUPER, A, ChatGPT, exec, omarchy-launch-webapp "https://chatgpt.com"
bindd = SUPER SHIFT, A, Grok, exec, omarchy-launch-webapp "https://grok.com"

# Window management overrides
bindd = SUPER, Q, Close active window, killactive,
unbind = SUPER, T
bindd = SUPER, T, Toggle float + center, exec, omarchy-cmd-toggle-float-center

# Overwrite existing bindings, like putting Omarchy Menu on Super + Space
# unbind = SUPER, SPACE
# bindd = SUPER, SPACE, Omarchy menu, exec, omarchy-menu
