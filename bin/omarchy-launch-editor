#!/bin/bash

E="${EDITOR:-nvim}"
if ! command -v "$E" >/dev/null 2>&1; then
  E="nvim"
fi

case "$E" in
  nvim | vim | nano | micro | hx)
    exec setsid uwsm app -- "$TERMINAL" -e "$E" "$@"
    ;;
  *)
    exec setsid uwsm app -- "$E" "$@"
    ;;
esac
